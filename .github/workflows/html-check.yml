name: HTML Check

on:
  push:
  pull_request:

jobs:
  validate-html:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Validate HTML
        uses: uses: Cyb3r-Jak3/html5validator-action@master
        with:
          root: .